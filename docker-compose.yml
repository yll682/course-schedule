version: '3.8'
services:
  course-schedule:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - db_data:/app          # 持久化数据库和日志
    env_file:
      - .env                  # 从 .env 文件读取环境变量
    restart: unless-stopped

volumes:
  db_data:
